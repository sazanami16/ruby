def find_roots(a, b, c)
  ax ^ 2 + bx + c = 0
end

def find_roots_sol1(a, b, c)
  find_roots = find_roots(a, b, c)
  if find_roots >= 0
    
  end
end

def find_roots_sol2(a, b, c)
  find_roots = find_roots(a, b, c)
  if find_roots >= 0
    
  end

def find_roots_eq(a, b, c)
  find_roots = find_roots(a, b, c)
  if find_roots > 0
    [find_roots_sol1(a, b, c), find_roots_sol2(a, b, c)]
  elsif find_roots = 0
  end
end

find_roots(2, 10, 8)
